package pe.edu.softwareii.doglove;

import android.os.Bundle;
import android.support.v7.app.AppCompatActivity;

public class PerfilActivity extends AppCompatActivity {
    


    //ApplicationController app;



    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_perfil);


        /*
        if (mascota.getRaza().equals("Labrador"))
            iviFoto.setImageResource(R.drawable.labrador);
        else
            iviFoto.setImageResource(R.drawable.chitzu);
        tviNombreEdad.setText(mascota.getNombreMascota()+", "+mascota.getEdad()+" años");
        tviDescripcion.setText(mascota.getDescripcion());
        tviNombreUsuario.setText(usuario.getNombre()+" "+usuario.getApellidop()+" "+ usuario.getApellidom());
        tviEmail.setText(usuario.getEmail());*/





    }
}
